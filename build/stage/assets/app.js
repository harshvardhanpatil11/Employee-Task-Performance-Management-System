document.addEventListener('DOMContentLoaded',()=>{document.querySelectorAll('form[data-validate]').forEach(form=>form.addEventListener('submit',event=>{if(!form.checkValidity()){event.preventDefault();event.stopPropagation()}form.classList.add('was-validated');const start=form.querySelector('[name="startDate"]'),due=form.querySelector('[name="dueDate"]');if(start&&due&&start.value&&due.value&&due.value<start.value){event.preventDefault();due.setCustomValidity('Due date must be on or after the start date.');due.reportValidity()}else if(due)due.setCustomValidity('')}));document.querySelectorAll('[name="startDate"]').forEach(start=>start.addEventListener('change',()=>{const due=start.form.querySelector('[name="dueDate"]');if(due){due.min=start.value;if(due.value&&due.value<start.value)due.value=start.value}}));document.querySelectorAll('a[href$="/logout"]').forEach(link=>link.addEventListener('click',event=>{if(!confirm('Sign out of your workspace?'))event.preventDefault()}))});
